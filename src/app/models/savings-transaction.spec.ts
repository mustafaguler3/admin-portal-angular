import { SavingsTransaction } from './savings-transaction';

describe('SavingsTransaction', () => {
  it('should create an instance', () => {
    expect(new SavingsTransaction()).toBeTruthy();
  });
});
